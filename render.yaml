services:
  - type: web
    name: yt-vault-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
